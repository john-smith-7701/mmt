% layout 'defrwt';
% title 'login' ;
<h2>Login</h2>
%= join '|',@{$self->req->url->path->parts},$ENV{MOJO_REVERSE_PROXY},@{$self->req->url->base->path->parts}
%= form_for './login' => (method => 'post') => begin
 <br>Name:
 %= text_field 'user',size => 60
 <br>password:
 %= text_field 'passwd',size => 60
 <br>
 %= submit_button 'Login'
 %= hidden_field url => $url 
% end
